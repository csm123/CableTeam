= form_for @document, :html => {:class => "pure-form pure-form-stacked"} do |f|
  = f.label :title
  = f.text_field :title
  = f.label :body
  = f.text_area :body
  = f.submit "Save Changes", {:class => "pure-button pure-button-primary"}
